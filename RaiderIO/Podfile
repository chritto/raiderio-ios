# Uncomment the next line to define a global platform for your project
platform :ios, '12.0'

target 'RaiderIO' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!

  # Pods for RaiderIO
  pod 'IGListKit', '~> 3.0'

  target 'RaiderIOTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
